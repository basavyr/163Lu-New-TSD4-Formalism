#include <algorithm>
#include <cmath>
#include <vector>

class rms
{
public:
    static std::vector<double> GenerateTheoreticalData(std::vector<double> &exp_data,)
    static double RMS(std::vector<double> &exp_data, std::vector<double> &th_data);
};
